import stringify from './qs/stringify';
interface Param {
    pathname: string;
    query?: Parameters<typeof stringify>[0];
}
/**
 * Call LIFF custom URL scheme
 * @export
 * @param {string} pathname - string
 * @param {Query} query - see ./qs/stringify
 * @returns {Promise<void>}
 */
export default function callLiffCustomURLScheme({ pathname, query, }: Param): Promise<void>;
export {};
