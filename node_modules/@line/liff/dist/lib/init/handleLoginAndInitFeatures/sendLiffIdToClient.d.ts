/**
 * pass liff id and feature_token to native-app with liff:// scheme and wait for the `liffIdReceived` event
 * @export
 * @param {string} liffId
 * @param {string} featureToken
 * @returns {{Promise<void>}}
 */
export default function sendLiffIdToClient(liffId: string, featureToken: string): Promise<void>;
