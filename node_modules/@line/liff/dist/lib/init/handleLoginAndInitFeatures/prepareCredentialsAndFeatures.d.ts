import { CREDENTIAL_OBJECTS } from '../../util/consts';
interface ReturnValue {
    credentials: CREDENTIAL_OBJECTS;
    features?: string[] | null;
}
export default function prepareCredentialsAndFeatures(liffId: string): Promise<ReturnValue>;
export {};
