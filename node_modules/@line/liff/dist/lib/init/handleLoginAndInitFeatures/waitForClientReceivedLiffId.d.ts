/**
 * Listen to `liffIdReceived` event from client
 * This event fired when a native-app detects LIFF ID via a custom URL scheme
 * @export
 * @returns {Promise<void>}
 */
export default function waitForClientReceivedLiffId(): Promise<void>;
